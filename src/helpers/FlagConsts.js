import ireland from "../images/ireland.png";
import iceland from "../images/iceland.png";
import canada from "../images/canada.png";

// Maps a `flagKey` stored on a courseInfo doc to the imported PNG asset.
// Add a new entry here whenever a course needs a flag we haven't used before.
export const flagImages = {
    ireland,
    iceland,
    canada,
};
